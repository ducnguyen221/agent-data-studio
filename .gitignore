# Secrets — KHÔNG commit (.env giữ luôn con trỏ POWERBI_PROJECT_DIR)
.env
.env.bak.*

# Virtual env
.venv/

# Python cache
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Backup do installer tạo
*.bak.*

# Output tạm khi chạy scripts sinh mindmap/excel trong skill
out/

# Tài liệu nội bộ (máy-cá-nhân, client context) — không public
docs/internal/

# Policy thật theo máy/dự án (cột PII nhạy cảm) — không public
policy.json

# Cache tool local
.ruff_cache/

# Con trỏ đời cũ (trước v0.6) — con trỏ mới là 1 dòng POWERBI_PROJECT_DIR trong .env
knowledge.config.json

# Residue cua bo test installer (fake USERPROFILE)
tests/installer/fakehome/
